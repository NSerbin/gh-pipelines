name: Terraform Init

on:
  workflow_call:
    inputs:
      terraform_version:
        required: true
        type: string
        default: 1.8.3


# jobs:
#     terraform_init:
#     name: Terraform Init
#     runs-on: ubuntu-latest
#     steps:
#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3.1.1
#         with:
#           terraform_version: ${{ inputs.terraform_version }}  
#       - name: Terraform Init
#         id: init
#         run: terraform init


jobs:
    terraform_init:
    name: Terraform Init
    runs-on: ubuntu-latest
    steps:
      - name: Prepare Terraform
        uses: ./.github/workflows/tf-prepare.yml
      - name: Terraform Init
        id: init
        run: terraform init
