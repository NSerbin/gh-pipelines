name: Terraform Init

on:
  workflow_call:
    inputs:
      terraform_version:
        required: true
        type: string
        default: 1.12.2
      working-directory:
        required: false
        type: string
        default: '.'

jobs:
  terraform_init:
    name: Terraform Init
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0
          ref: "${{ github.head_ref || github.ref }}"
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2
        with:
          terraform_version: ${{ inputs.terraform_version }}
      - name: Export secret as env
        if: ${{ env.secret != 'null' }}
        run: echo "$secret" | jq -r 'to_entries[] | "\(.key)=\(.value)"' >> $GITHUB_ENV
        env: 
          secret: ${{ toJson(secrets) }}
      - name: Export vars as env
        if: ${{ env.var != 'null' }}
        env:
          var: ${{ toJson(vars) }}
        run: echo "$var" | jq -r 'to_entries[] | "\(.key)=\(.value)"' >> $GITHUB_ENV
      - name: Replace ${VARS} in *.tfvars with env values
        shell: bash
        run: |
          set -euo pipefail

          # Asegura que envsubst esté disponible (ya viene en ubuntu-latest; esto es por si acaso)
          if ! command -v envsubst >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y gettext-base
          fi

          # Recorre todos los .tfvars de forma segura (maneja espacios/NUL)
          while IFS= read -r -d '' file; do
            echo "🔍 Processing $file"

            # Extrae placeholders tipo ${VAR} únicos (nombres válidos de env: [A-Z0-9_])
            # Evitamos -P para no depender de PCRE en GNU grep; usamos BRE con \{,\}
            mapfile -t matches < <(grep -o '\${[A-Za-z_][A-Za-z0-9_]*}' "$file" | sort -u || true)

            # Construye la lista de variables para envsubst SOLO con las que estén seteadas y no vacías
            varlist=()
            for m in "${matches[@]}"; do
              varname="${m:2:${#m}-3}" # quita ${ y }
              # ¿Está seteada y no vacía?
              if [[ -n "${!varname-}" ]]; then
                varlist+=("$m")
              else
                echo "⚠️  $varname is not set in environment. Skipping..."
              fi
            done

            if [[ ${#varlist[@]} -eq 0 ]]; then
              echo "ℹ️  No variables to substitute in $file"
              continue
            fi

            # Ejecuta envsubst sólo con esas variables (no revela valores en logs)
            # Usamos un tmp y luego mv para evitar archivos truncados si algo falla
            tmp="$(mktemp)"
            # shellcheck disable=SC2046 # queremos expandir como lista separada por espacios
            envsubst "$(printf ' %s' "${varlist[@]}")" < "$file" > "$tmp"
            mv "$tmp" "$file"

            echo "✅ Substituted ${#varlist[@]} variable(s) in $file"
          done < <(find . -name '*.tfvars' -print0)
      - name: Download and install cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/cloudflared
      - name: Start Cloudflare Tunnel (background, debug)
        run: |
          set -euo pipefail
          nohup cloudflared access tcp \
            --hostname tf-backend.nserbin.com \
            --url localhost:5433 \
            --id $"{{ secrets.CF_ACCESS_CLIENT_SECRET }} "\
            --secret "${{ secrets.CF_ACCESS_CLIENT_ID }}" \
            --loglevel debug \
            --logfile cloudflared.log \
            > /dev/null 2>&1 &
          # Espera a que escuche 5433
          for i in {1..40}; do
            if (echo > /dev/tcp/127.0.0.1/5433) >/dev/null 2>&1; then
              echo "✅ Tunnel is ready on 5433"; exit 0
            fi
            sleep 1
          done
          echo "❌ Tunnel not ready. Dumping cloudflared.log:"; sed -n '1,200p' cloudflared.log || true
          exit 1

      # ------- Smoke test a la DB (asegura conectividad real) -------
      - name: Install psql client
        run: sudo apt-get update -y && sudo apt-get install -y postgresql-client

      - name: Smoke test Postgres via tunnel
        env:
          PGURL: postgres://${{ secrets.BACKEND_USER }}:${{ secrets.BACKEND_PASSWORD }}@localhost:5433/${{ secrets.BACKEND_DB }}?sslmode=disable
        run: |
          set -euo pipefail
          echo "↪️  Listing state-like tables (sanity check)..."
          psql "$PGURL" -c "SELECT schemaname, relname FROM pg_catalog.pg_tables WHERE relname ILIKE '%state%';" -q || { echo "❌ cannot query DB"; exit 1; }

      # ------- Re-init con backend + workspace + ver state -------
      - name: Terraform Init (reconfigure)
        working-directory: ${{ inputs.working-directory }}
        run: |
          terraform init\
            -backend-config="conn_str=postgres://${{ secrets.BACKEND_USER }}:${{ secrets.BACKEND_PASSWORD }}@localhost:5433/${{ secrets.BACKEND_DB }}?sslmode=disable"
      - name: Tar Terraform .terraform directory
        run: |
          tar -czf terraform-cache.tar.gz .terraform .terraform.lock.hcl
        working-directory: ${{ inputs.working-directory }}
      - name: Save .terraform directory
        uses: actions/upload-artifact@v4.6.2
        with:
          name: terraform-cache
          include-hidden-files: true
          path: ${{ inputs.working-directory }}/terraform-cache.tar.gz
